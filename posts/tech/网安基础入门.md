---
title: '网安基础入门'
date: '2023-02-05'
description: 
---


网安

1，ip地址：网段加x（255，0）
2，子网掩码：（255，0，0，0）
3，第一位地址分5类:a类1-126；b类：128-191；c类：192-223；（后面不用）d类：224-239；e类：225-254（1，127这一段不用，是回环地址）（2，255这一点不能用，（主机位不能全为0或255）是广播，是对该网段内所有人沟通），全为0是网段名字，
4，d段组播，多方只发一遍
5，e段科研
6，网关地址一般用第一个或者最后一个，网关用来上外网（不在同一网段）：一般就是intel和realtek
7，DNS：
8，ping ip地址：测试连通性
ping -t：一直ping，直到按control+c；pinh -n 数字 ：ping几个包（默认ping四个包）；ping -l 数字 ip地址：修改ping包大小
9，查看ip地址；ipconfig  /all
0，windows r 输入 calc 计算器 
mspaint 画板
￼
0，dos命令：
1，｜more：按空格键翻页查看
2，type 文件名，浏览文件 
3，>content>文件名：创建文件；copy con 文件名：创建多内容，control z结束
4，cls：清屏
5，dir：浏览文件目录 /a：全部 隐藏的也有
6，批量删 del *.拓展名 *.*：全删
7，/q：不提醒直接执行
8，/s：全删
9，attrib：加属性 +h（文件夹隐藏）+s：升级文件夹为系统文件夹 +a：不能够读写
0，md：创建文件夹
1，fsutil file createnew：生出文件 后面写路径加文件名 后面加大小
2，assoc：修改关联性 .拓展名a=拓展名b +file 
3，shutdown -s或者/s -t 几秒
shutdown -a取消一切定时
shutdown -r -t定时重启
有时候不好使 加-f ；加-c“”里面可以编辑消息；-l：注销
4，copy 文件1 文件2 
5，..\走出当前文件
6，.\当前文件
7，move移动 用法同copy
8，ren 文件1 文件2:改名
9，\\ ip\：跨网络传输
0，批处理：从上至下处理每一条命令
拓展名.bat
1，pause 停止
2，@echo off：关闭回显
3，echo.：空一行
4，title：
5，>nul 2>nul：没有显示结果
6，1: ：区块 和goto搭配；goto 3
7，start：开始，后面什么都不跟就是打开cmd
8，%userprofile%：取出用户家目录
9，有特殊符号 前后加双引号
0，exit 退出
1，set /p a=：等待输入
2，if 语句 
用户管理
1，SID：最后一位是UID
2，windows管理员id是500开始，用户的uid是1000开始
3，用户账号密码储存位置：c：\windows\system32\config\SAM（哈希米算法储存）服务器 密码最长有效值默认42；撞库 或 暴力破解
4，document and settings 是用户的家
4，用管理员账号打开netplwiz
5，net user：查看所有用户；net localgroup：查看所有组
登陆管理员账号：net user administrator /active：yes；
6，net user 用户名 xxx （/add：改密码，直接双引号清空密码，（没有用户新建一个
7，组管理：同权限；内置组，系统默认；
8，踢了 /del
服务器远程管理
￼
￼
￼
￼
命令行方式控制
1，输入services.msc
2，打开telnet
netstat -an：查看本机开放的所有端口
telnet：23；远程桌面是：3389；
破解win7密码
1，5次shift，出现粘滞键提示 
2，让windows意外关机，出现错误提示界面，通过超链接进入，把cmd改sethc
NTFS 安全权限
1，文件系统：存储格式
2，block里面格子 文件;inote 文件名
3，acl：访问控制
4，出现系统错误5，权限不够
5，取消继承 强制继承（无法拒绝） 权限累加 拒绝最大 管理员可以取得所有权
6，defaultaccount：默认账号
文件共享服务器
1，cifs协议
2，cmd // 地址：访问


